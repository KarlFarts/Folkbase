# Content Security Policy and Security Headers
# This file is used by hosting platforms like Netlify and Vercel
# to configure HTTP headers for enhanced security

# Global security headers for all routes
/*
  # Content Security Policy (CSP)
  # Protects against XSS, clickjacking, and other code injection attacks
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' https://accounts.google.com https://apis.google.com https://www.googletagmanager.com https://www.google-analytics.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://accounts.google.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https:; connect-src 'self' https://accounts.google.com https://oauth2.googleapis.com https://sheets.googleapis.com https://www.googleapis.com https://o4508099178168320.ingest.us.sentry.io; frame-src https://accounts.google.com; frame-ancestors 'none'; base-uri 'self'; form-action 'self' https://accounts.google.com

  # Force HTTPS
  Strict-Transport-Security: max-age=63072000; includeSubDomains; preload

  # Prevent MIME type sniffing
  X-Content-Type-Options: nosniff

  # Enable XSS filter in browsers
  X-XSS-Protection: 1; mode=block

  # Prevent clickjacking
  X-Frame-Options: DENY

  # Referrer policy for privacy
  Referrer-Policy: strict-origin-when-cross-origin

  # Permissions policy (feature policy)
  Permissions-Policy: camera=(), microphone=(), geolocation=(), interest-cohort=()

  # Cache control for security (override for specific assets below)
  Cache-Control: no-cache, no-store, must-revalidate

# Static assets can be cached
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# Service worker (if added later)
/sw.js
  Cache-Control: no-cache, no-store, must-revalidate
